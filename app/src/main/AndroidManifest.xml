<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aks.evaccinationapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".totalReportActivity"
            android:parentActivityName=".AdminActivity"
            ></activity>
        <activity android:name=".VaccineReminderActivity"
            android:parentActivityName=".ParentActivity"
            />
        <activity
            android:name=".AssignedReportActivity"
            android:parentActivityName=".AdminActivity" />
        <activity
            android:name=".CreateAppointmentActivity"
            android:parentActivityName=".AdminActivity" />
        <activity
            android:name=".AdminCurrentApplicationsActivity"
            android:parentActivityName=".AdminActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AdminSignInActivity" />
        <activity
            android:name=".UpdateChildActivity"
            android:parentActivityName=".ChildrenActivity" />
        <activity
            android:name=".ChildrenActivity"
            android:parentActivityName=".ParentActivity" />
        <activity
            android:name=".AddChildDetailsActivity"
            android:parentActivityName=".ParentActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignInPage" />
        <activity android:name=".ParentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>